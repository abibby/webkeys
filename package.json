{
  "name": "webkeys",
  "version": "1.0.0",
  "description": "",
  "main": "webkeys.js",
  "scripts": {
    "build": "npm run build:define && npm run build:src && npm run bundle",
    "build:src": "tsc",
    "build:define": "tsc lib/define.ts --lib es2015,dom --outDir tmp",
    "bundle": "mkdir -p dist && cat lib/meta.js tmp/define.js tmp/out.js > dist/out.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.4.3"
  }
}
